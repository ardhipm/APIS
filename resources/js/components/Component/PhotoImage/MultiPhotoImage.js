import React from 'react';

const MultiPhotoImage = (props) => {
    const tabTitle = props.tabTitle;
    const selectedPicture = props.selectedPicture;
    const totalPicture = props.totalPicture;
    
    return(
        <div>test</div>
    );
}

export default MultiPhotoImage;